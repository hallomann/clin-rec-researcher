# ClinRecResearcher
Инструмент для быстрого поиска клинических рекомендаций Минздрава РФ по кодам МКБ-10, ориентированный на акушеров-гинекологов. Позволяет собирать, структурировать и отображать рекомендации с ссылками. MVP реализован на Flask + CSV — без сложных зависимостей, легко разворачивается локально.


## 🎯 Цель проекта

Обеспечить врачу удобство для того чтобы:
- Находить нужную рекомендацию по коду МКБ-10 за секунды.
- Видеть все релевантные рекомендации (если одна болезнь описана в нескольких документах).
- Иметь возможность расширять базу самостоятельно, без участия программиста.

## 🚀 Установка и запуск

1. Откройте терминал и склонируйте репозиторий через HTTPS:
   ```bash
   git clone https://github.com/ваш_логин/clin-rec-researcher.git
   ```
2. Перейдите в папку проекта
   ```
   cd clin-rec-researcher
   ```
3. Установите зависимости (проверьте, что у вас установлен [Python](https://www.python.org/downloads/)):
    ```
    pip install -r requirements.txt
    ```
4. Запустите веб-приложение:
    ```
    flask run
    ```
    после чего в терминале появится ссылка вида: http://localhost:5000 для открытия в браузере

## 🛠 Стэк

- Python
    - Selenium
    - Flask
    - pandas
- Jinja2
- bootstrap 5

## Структура проекта
```
clin-rec-researcher/
├── app.py                 # Веб-сервер на Flask (запускает интерфейс)
├── requirements.txt       # Зависимости
├── data/
│   ├── out.csv            # Основная база данных: ID, название, ссылка, МКБ-10
│   └── clindict.py        # Словарь: код МКБ → расшифровка
├── templates/             # HTML-шаблоны интерфейса
│   ├── base.html
│   ├── index.html         # Главная страница с поиском
│   └── search.html        # Страница результатов
├── static/                # Стили (Bootstrap по CDN)
├── adtocsv2.py            # Внутренний скрипт: парсит ссылку и добавляет в out.csv
├── readclinrec.py         # Парсер страниц (работает через Selenium)
├── massdict.py            # Скрипт для добавления новых кодов МКБ в словарь
└── README.md              # Документация
```
База данных обычный CSV-файл - его можно открыть в Excel, редактировать, резервировать и передавать.

Каждая строка = одна рекомендация + один код МКБ-10 (если у рекомендации несколько кодов - она дублируется построчно)
![alt text](static\img\image.png)

## Как добавить новую клиническую рекомендацию?
1. Откройте терминал в папке проекта.
2. Выполните:
```bash
python add_recommendation.py
```
3. Вставьте ссылку на рекомендацию с сайта Минздрава, например:
https://cr.minzdrav.gov.ru/view-cr/978_1

Это внешний скрипт и он:

    - Запустит `adtocsv2.py`, автоматически извлечёт название, ID и коды МКБ-10.
    - Добавит данные в data/out.csv.
    - Если какой-то код МКБ неизвестен — сообщит об этом и предложит использовать `massdict.py`.

4. Перезапустите `app.py`, чтобы изменения вступили в силу.

⚠️ Важно ⚠️ Ссылка должна быть с домена `cr.minzdrav.gov.ru` вида как в примере

Также Вы можете добавить новую запись в Вашу базу в `data/out.csv` вручную
